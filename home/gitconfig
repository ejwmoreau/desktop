[user]
  name = Elie Moreau
  email = emoreau@atlassian.com

# Override https urls with ssh ones
[url "ssh://git@stash.atlassian.com:7997/"]
  insteadOf = https://stash.atlassian.com/scm/
[url "git@bitbucket.org:"]
  insteadOf = https://bitbucket.org/
[url "git@github.com:"]
  insteadOf = https://github.com/

# Config for common git operations
[init]
  defaultBranch = main
[push]
  default = current
[pull]
  rebase = true

# Config for visual elements of git
[core]
  editor = vim
[color]
  interactive = auto
  branch = auto
  diff = auto
  status = auto

# Config for git lfs support
[filter "lfs"]
  required = true
  clean = git-lfs clean -- %f
  smudge = git-lfs smudge -- %f
  process = git-lfs filter-process

# Config for git-delta, which provides pretty diffs
# TODO: Merge both delta modes so they look the same in both `git diff` and `git add -p`
# TODO: Use base16 tomorrow-dark colours (try to find them via `delta --show-colors`
#[config]
#  pager = delta --color-only --features=interactive
#[interactive]
#  diffFilter = delta --color-only --features=interactive
#[delta]
#  syntax-theme = base16
#  side-by-side = true
#  features = decorations
#  keep-plus-minus-markers = false
#[delta "interactive"]
#  keep-plus-minus-markers = false
#[delta "decorations"]
#  commit-decoration-style = blue ol
#  commit-style = raw
#  file-style = omit
#  hunk-header-decoration-style = blue box
#  hunk-header-file-style = red
#  hunk-header-line-number-style = "#067a00"
#  hunk-header-style = file line-number syntax

# Config for diff-so-fancy: https://formulae.brew.sh/formula/diff-so-fancy
[core]
  pager = diff-so-fancy | less --tabs=4 -RF
[interactive]
  diffFilter = diff-so-fancy --patch
