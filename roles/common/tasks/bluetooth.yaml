---
- name: Install Bluetooth packages
  become: true
  pacman:
    name:
      - bluez
      - bluez-utils

# TODO: Install via yay
# - bluetooth-headset-battery-level-git

- name: Enable Bluetooth
  become: true
  systemd:
    name: bluetooth
    enabled: yes
